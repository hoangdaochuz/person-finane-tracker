apiVersion: v1
kind: Service
metadata:
  name: finance-tracker-service
  namespace: default
  labels:
    app: finance-tracker
    component: backend
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: finance-tracker
    component: backend
